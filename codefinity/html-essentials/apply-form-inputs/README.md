# Personal Information Form

A simple HTML project that creates a personal information form with different input types.

## What I practiced

* Creating an HTML form
* Using different input types: `text`, `email`, `password`, and `date`
* Connecting labels with inputs using `for` and `id`
* Adding required fields
* Creating a submit button

## Technologies

* HTML

## Status

Completed as part of HTML practice.


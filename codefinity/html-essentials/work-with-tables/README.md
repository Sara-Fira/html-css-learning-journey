# Restaurant Menu

A simple HTML project that displays a restaurant menu using a table.

## What I practiced

* Creating an HTML document structure
* Building a table with `table`, `thead`, `tbody`, `tr`, `th`, and `td`
* Using basic text formatting tags like `strong` and `em`
* Organizing menu items into rows and columns

## Technologies

* HTML

## Status

Completed as part of HTML practice.

